name: Diagnose GitHub-IP

# Einmalige Untersuchung: Welche Shops weisen die GitHub-Runner-IP ab, und
# behebt ein anderer Egress (Cloudflare WARP) das? Beide Arme laufen auf
# ubuntu-latest, damit der Vergleich sauber ist. Nur manuell auslösbar.
on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        methode: [direkt, warp]
    name: ${{ matrix.methode }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Abhängigkeiten
        run: pip install -r requirements.txt

      - name: Cloudflare WARP einrichten
        if: matrix.methode == 'warp'
        run: |
          set -e
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg \
            | sudo gpg --yes --dearmor -o /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" \
            | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update
          sudo apt-get install -y cloudflare-warp
          sudo systemctl start warp-svc || true
          sleep 3
          # CLI-Syntax hat sich zwischen Versionen geändert - beide Wege versuchen.
          sudo warp-cli --accept-tos registration new \
            || sudo warp-cli --accept-tos register \
            || sudo warp-cli register || true
          sudo warp-cli --accept-tos connect || sudo warp-cli connect
          sleep 8
          echo "--- Trace ---"
          curl -s https://www.cloudflare.com/cdn-cgi/trace | grep -E 'warp=|ip=' || true
          # Abbrechen, wenn WARP NICHT aktiv ist - sonst misst dieser Arm nur
          # wieder die nackte Runner-IP und täuscht ein Ergebnis vor.
          if ! curl -s https://www.cloudflare.com/cdn-cgi/trace | grep -q 'warp=on'; then
            echo "::error::WARP nicht aktiv (warp=on fehlt) - Diagnose-Arm ungültig."
            exit 1
          fi

      - name: Egress-IP
        run: |
          echo "Egress-IP: $(curl -s https://api.ipify.org)"

      - name: Shops prüfen (${{ matrix.methode }})
        run: python tools/diagnose_shops.py
