# HTTP-Cache und Laufzeitartefakte
.cache/
run.log
audit.log

# Generierte Ausgaben - entstehen bei jedem Lauf neu
bericht.html
deals.json

# Preisverlauf: maschinenlokale Datensammlung, kein Quellcode.
# Wer die Historie mitversionieren will, nimmt die Zeile raus.
preise.db

# Bewertungs-Cache (wird wöchentlich neu geholt). Die manuell gepflegte
# Variante ist dagegen bewusst nicht ignoriert - siehe .beispiel.json.
bewertungen.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
